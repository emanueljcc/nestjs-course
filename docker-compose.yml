version: '3.8'
services:
  db:
    image: postgres
    restart: always
    environment:
      POSTGRES_DB: 'postgres_db'
      POSTGRES_USER: 'db_user'
      POSTGRES_PASSWORD: 'password'
    volumes:
      - ./data:/var/lib/postgresql/data
    ports:
      - 5499:5432